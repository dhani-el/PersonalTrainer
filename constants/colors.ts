const primaryBackGround = "#0B0B0B"; //jet black
const primaryText = "#FFFFFF"; //white
const accent = "#7CFC00"; //neon green
const active = "#39FF14"; //neon green
const secondaryText = "#A1A1A1"; //cool gray
const accentText = "#000000"; //black
const errorColor = "FF3B3B"; // warning Red
const successColor = "2DFF6A"; //success green

const colors = {
  primaryBackGround,
  primaryText,
  accent,
  active,
  secondaryText,
  errorColor,
  successColor,
  accentText,
};

export default colors;
